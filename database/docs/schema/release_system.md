# release_system

## Description

<details>
<summary><strong>Table Definition</strong></summary>

```sql
CREATE TABLE "release_system" (
    release_id INTEGER NOT NULL,
    system_id INTEGER NOT NULL,
    PRIMARY KEY (release_id, system_id),
    FOREIGN KEY (release_id) REFERENCES release(id) ON DELETE CASCADE,
    FOREIGN KEY (system_id) REFERENCES system(id) ON DELETE CASCADE
)
```

</details>

## Columns

| Name | Type | Default | Nullable | Children | Parents | Comment |
| ---- | ---- | ------- | -------- | -------- | ------- | ------- |
| release_id | INTEGER |  | false |  | [release](release.md) |  |
| system_id | INTEGER |  | false |  | [system](system.md) |  |

## Constraints

| Name | Type | Definition |
| ---- | ---- | ---------- |
| release_id | PRIMARY KEY | PRIMARY KEY (release_id) |
| system_id | PRIMARY KEY | PRIMARY KEY (system_id) |
| - (Foreign key ID: 0) | FOREIGN KEY | FOREIGN KEY (system_id) REFERENCES system (id) ON UPDATE NO ACTION ON DELETE CASCADE MATCH NONE |
| - (Foreign key ID: 1) | FOREIGN KEY | FOREIGN KEY (release_id) REFERENCES release (id) ON UPDATE NO ACTION ON DELETE CASCADE MATCH NONE |
| sqlite_autoindex_release_system_1 | PRIMARY KEY | PRIMARY KEY (release_id, system_id) |

## Indexes

| Name | Definition |
| ---- | ---------- |
| sqlite_autoindex_release_system_1 | PRIMARY KEY (release_id, system_id) |

## Relations

![er](release_system.svg)

---

> Generated by [tbls](https://github.com/k1LoW/tbls)
